<% hatalar.forEach(element => { %>
    <div class="alert alert-danger" role="alert">
        <%- element.msg %> 
      </div>
<% }) %>

<% errorFlashMessage.forEach(element => { %>
  <div class="alert alert-danger" role="alert">
      <%- element %> 
    </div>
<% }) %>
